import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  header: {
    justifyContent: 'center',
    alignItems: 'center',
    borderBottomWidth: 1,
    marginTop: 50,
    paddingBottom: 15,
    borderColor: '#EBEBEB',
  },
  headerText: {
    fontSize: 20,
    fontWeight: '500',
  },
});
